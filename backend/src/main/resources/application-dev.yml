spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/agrocortex
    username: agrocortex
    password: agrocortex
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    com.agrocortex: DEBUG
    org.hibernate.SQL: DEBUG
