# === Backend (Spring Boot) ===
SPRING_PROFILES_ACTIVE=dev

# PostgreSQL
DATABASE_URL=jdbc:postgresql://localhost:5432/agrocortex
DB_USERNAME=agrocortex
DB_PASSWORD=agrocortex

# JWT / Security (futuro)
JWT_SECRET=
JWT_EXPIRATION_MS=86400000

# === Frontend (Angular) ===
API_URL=http://localhost:8080/api
